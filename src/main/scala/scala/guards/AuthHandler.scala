package scala.guards

import io.vertx.ext.web.handler.BasicAuthHandler

object AuthHandler {
  // router.route().handler(SessionHandler.create(LocalSessionStore.create(vertx)));

  // var basicAuthHandler = BasicAuthHandler.create(authProvider);
}
